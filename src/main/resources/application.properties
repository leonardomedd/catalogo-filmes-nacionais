# Configurações padrão (local)
spring.datasource.url=jdbc:postgresql://localhost:5432/catalogo_filmes
spring.datasource.username=postgres
spring.datasource.password=admin422
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
server.port=8081

# Configuração para ambiente de produção (Railway)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/catalogo_filmes}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:admin422}